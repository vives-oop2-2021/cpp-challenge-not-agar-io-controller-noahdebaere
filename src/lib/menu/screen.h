#pragma once
#include <string>

namespace NotAgarIOController {

    enum class Screen {
        MAIN_MENU,
        PLAY,
        DIFFICULTY,
        USERS,
        REGISTER,
        SCOREBOARD,
        HELP,
        EXIT,
        NOT_SET
    };
};