#pragma once
#include <string>

namespace NotAgarIOController {

    enum class Commands {
        EXIT,
        HELP,
        REGISTER,
        PLAYERS,
        MOVE,
        SHOOT,
        UNKNOWN
    };
};